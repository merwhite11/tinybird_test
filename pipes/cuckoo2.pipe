TOKEN "cuckoo2_pipe_endpoint_read_2796" READ

NODE get_count_by_date
SQL >

    SELECT
        toDate(date) AS date,
        countState(*) AS row_count
    FROM cli_source
    GROUP BY date

TYPE materialized
DATASOURCE count_by_date


